spring.application.name=healthbot

# ------------------------
# Server Config
# ------------------------
server.port=8080

# ------------------------
# H2 Database Config
# ------------------------
spring.datasource.url=jdbc:h2:mem:healthbotdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update

# ------------------------
# H2 Console (Web UI)
# ------------------------
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# ------------------------
# Twilio Config (replace with real values)
# ------------------------
twilio.whatsapp.number=+14155238886  # Example WhatsApp sandbox number